import {useState, useEffect} from "react";


const Buckets = (props) => {
    const [bucketList, setBucketList] = useState([]);

    return (
        <>
            Bucket list del usuario loggeado
        </>
    )
}

export default Buckets;